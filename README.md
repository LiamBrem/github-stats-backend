# GitHub Leaderboard Backend

This service collects contribution data from GitHub repositories and stores it in a PostgreSQL database (Supabase). It is designed to be triggered by GitHub Actions on merged pull requests and submitted code reviews.
